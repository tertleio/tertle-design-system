import { Container } from '@/cmps/Container';
import { MainLayout } from '@/cmps/Layouts';

const Profile = () => {
  return (
    <MainLayout>
      <Container>Profile</Container>
    </MainLayout>
  );
};

export { Profile };
